# SCSS - custom selection

To change the effect when you select text on the page:

```scss
// usually in _base.scss file:

::selection {
  background-color: #1818181;
  color: #fff;
}
```
